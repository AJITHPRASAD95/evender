const { mongoose, orderItemSchema } = require('./common');
const schema = new mongoose.Schema({ masterOrderId: { type: mongoose.Schema.Types.ObjectId, ref: 'MasterOrder', required: true, index: true }, shopId: { type: mongoose.Schema.Types.ObjectId, ref: 'Shop', required: true, index: true }, items: [orderItemSchema], subtotal: Number, commissionAmount: Number, vendorPayableAmount: Number, status: { type: String, enum: ['pending','accepted','preparing','ready_for_pickup','picked_up','delivered','rejected','cancelled'], default: 'pending' }, assignedDeliveryPartnerId: { type: mongoose.Schema.Types.ObjectId, ref: 'DeliveryPartner' }, estimatedPreparationTime: Number, refundStatus: { type: String, enum: ['not_applicable','pending','processed','failed'], default: 'not_applicable' } }, { timestamps: true });
module.exports = mongoose.model('SubOrder', schema);
