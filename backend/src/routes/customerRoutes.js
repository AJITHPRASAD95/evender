const router=require('express').Router();const Customer=require('../models/Customer');router.get('/me',async(req,res,next)=>{try{res.json({success:true,data:await Customer.findById(req.user.id)});}catch(e){next(e)}});router.patch('/me',async(req,res,next)=>{try{const allowed=['name','email','addresses','defaultLocation','fcmToken'];const patch=Object.fromEntries(Object.entries(req.body).filter(([k])=>allowed.includes(k)));res.json({success:true,data:await Customer.findByIdAndUpdate(req.user.id,patch,{new:true,runValidators:true})});}catch(e){next(e)}});module.exports=router;
