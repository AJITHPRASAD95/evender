const router=require('express').Router();const Shop=require('../models/Shop');const {distanceKm}=require('../utils/distanceCalculator');router.get('/nearby',async(req,res,next)=>{try{const location={latitude:Number(req.query.latitude),longitude:Number(req.query.longitude)};const shops=(await Shop.find({status:'approved',isOpen:true})).filter(s=>distanceKm(s.location,location)<=s.deliveryRadiusKm);res.json({success:true,data:shops});}catch(e){next(e)}});module.exports=router;
