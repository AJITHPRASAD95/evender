{
  "name": "freshlane-render-entrypoint",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix backend ci",
    "start": "npm --prefix backend start"
  },
  "engines": {
    "node": ">=20"
  }
}
